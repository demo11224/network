<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.SysRoleMapper">

    <resultMap type="SysRole" id="SysRoleResult">
        <id property="roleId" column="role_id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="roleName" column="role_name"/>
        <result property="roleKey" column="role_key"/>
        <result property="roleSort" column="role_sort"/>
        <result property="dataScope" column="data_scope"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectRoleVo">
        select r.role_id, r.tenant_id, r.role_name, r.role_key, r.role_sort, r.data_scope, r.status, r.del_flag, r.create_by, r.create_time, r.remark
        from sys_role r
    </sql>

    <select id="selectRolesByUserId" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        left join sys_user_role ur on ur.role_id = r.role_id
        where ur.user_id = #{userId} and r.del_flag = 0
    </select>

    <select id="selectRoleList" parameterType="SysRole" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.del_flag = 0
        <if test="roleName != null and roleName != ''">
            AND r.role_name like concat('%', #{roleName}, '%')
        </if>
        <if test="roleKey != null and roleKey != ''">
            AND r.role_key like concat('%', #{roleKey}, '%')
        </if>
        <if test="status != null and status != ''">
            AND r.status = #{status}
        </if>
        order by r.role_sort
    </select>

    <select id="selectRoleById" parameterType="Long" resultMap="SysRoleResult">
        <include refid="selectRoleVo"/>
        where r.role_id = #{roleId} and r.del_flag = 0
    </select>

    <select id="checkRoleKeyUnique" parameterType="String" resultType="int">
        select count(1) from sys_role where role_key = #{roleKey} and del_flag = 0 limit 1
    </select>
</mapper>
