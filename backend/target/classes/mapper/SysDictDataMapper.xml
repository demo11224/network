<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.SysDictDataMapper">
    <select id="selectDictDataList" parameterType="SysDictData" resultType="SysDictData">
        select dict_code, dict_sort, dict_label, dict_value, dict_type, css_class, list_class, is_default, status, create_by, create_time, remark
        from sys_dict_data
        <where>
            <if test="dictType != null and dictType != ''">
                AND dict_type = #{dictType}
            </if>
            <if test="dictLabel != null and dictLabel != ''">
                AND dict_label like concat('%', #{dictLabel}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by dict_sort asc
    </select>

    <select id="selectDictDataByType" parameterType="String" resultType="SysDictData">
        select dict_code, dict_sort, dict_label, dict_value, dict_type, css_class, list_class, is_default, status
        from sys_dict_data where dict_type = #{dictType} and status = '0'
        order by dict_sort asc
    </select>
</mapper>
