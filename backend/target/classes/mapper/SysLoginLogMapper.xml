<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.SysLoginLogMapper">
    <select id="selectLoginLogList" parameterType="SysLoginLog" resultType="SysLoginLog">
        select info_id, tenant_id, user_name, ipaddr, login_location, browser, os, status, msg, login_time
        from sys_login_log
        <where>
            <if test="userName != null and userName != ''">
                AND user_name like concat('%', #{userName}, '%')
            </if>
            <if test="ipaddr != null and ipaddr != ''">
                AND ipaddr like concat('%', #{ipaddr}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        order by login_time desc
    </select>
</mapper>
