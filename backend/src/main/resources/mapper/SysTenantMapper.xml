<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.admin.mapper.SysTenantMapper">
    <select id="selectTenantList" parameterType="SysTenant" resultType="SysTenant">
        select tenant_id, tenant_name, contact_name, contact_phone, status, domain, package_id, account_count, expire_time, create_by, create_time, remark
        from sys_tenant
        <where>
            <if test="tenantName != null and tenantName != ''">
                AND tenant_name like concat('%', #{tenantName}, '%')
            </if>
            <if test="contactName != null and contactName != ''">
                AND contact_name like concat('%', #{contactName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>
</mapper>
