# 企业级 RAG 落地执行手册

> 面向"几千上万份各种格式文档"的检索增强问答(RAG)系统落地。
> 目标读者:要把 RAG 真正做上线的项目负责人 / 工程团队。
> 核心立场:**通俗易懂、逻辑清晰、可落地、客观且有批判性**。本手册不卖幻想,会明确告诉你哪些是常见误区、哪些是天生做不到的事。

---

## 目录

- [第 0 章 先纠正三个致命认知](#第-0-章-先纠正三个致命认知)
- [第 1 章 RAG 全链路原理(为什么演示惊艳、上线拉胯)](#第-1-章-rag-全链路原理为什么演示惊艳上线拉胯)
- [第 1.5 章 文档收集阶段:怎么收、定什么标准、各格式怎么要](#第-15-章-文档收集阶段怎么收定什么标准各格式怎么要)
- [第 2 章 切分前 / 中 / 后该做什么](#第-2-章-切分前--中--后该做什么)
- [第 2.5 章 各格式原始文档"解析+清洗"傻瓜式手册(入 RAGFlow 切分之前)](#第-25-章-各格式原始文档解析清洗傻瓜式手册入-ragflow-切分之前)
- [第 3 章 检索与生成:决定准确率的另一半](#第-3-章-检索与生成决定准确率的另一半)
- [第 3.5 章 Embedding 与 Rerank 模型选型:文本还是多模态](#第-35-章-embedding-与-rerank-模型选型文本还是多模态)
- [第 4 章 "95% 准确率"怎么定义和验证](#第-4-章-95-准确率怎么定义和验证)
- [第 5 章 产品选型:扣子 / Dify / RAGFlow 原理与对比](#第-5-章-产品选型扣子--dify--ragflow-原理与对比)
- [第 6 章 RAGFlow 分块方法详解(最新版)](#第-6-章-ragflow-分块方法详解最新版)
- [第 7 章 解析工具:MarkItDown vs MinerU 深度对比](#第-7-章-解析工具markitdown-vs-mineru-深度对比)
- [第 8 章 关键决策:到底要不要先转 Markdown?](#第-8-章-关键决策到底要不要先转-markdown)
- [第 9 章 MinerU → Markdown → RAGFlow 落地标准](#第-9-章-mineru--markdown--ragflow-落地标准)
- [第 10 章 端到端落地路线图(9 个阶段)](#第-10-章-端到端落地路线图9-个阶段)
- [第 11 章 最容易被忽视的 60%(权限/安全/运营等)](#第-11-章-最容易被忽视的-60权限安全运营等)
- [第 12 章 RAG 能力边界(必须管理预期)](#第-12-章-rag-能力边界必须管理预期)
- [附录 A 工具选型速查](#附录-a-工具选型速查)
- [附录 B 落地优先级](#附录-b-落地优先级)
- [附录 C 避坑总纲](#附录-c-避坑总纲)

---

## 第 0 章 先纠正三个致命认知

在动手之前,先记住三句话,它们能帮你避开 80% 的坑:

1. **"把 RAG 准确率做到 95%" 这个目标本身不严谨。**
   不先定义"准确率指的是什么",这个数字没有任何工程意义。RAG 有检索和生成两个阶段,各有各的指标,用一个含糊的 95% 是无法验收的。(详见第 4 章)

2. **RAG 不是"切分得好就准"。**
   失败的最大单一来源不是切分,而是**文档解析**。几千上万份"各种格式"文档,意味着扫描 PDF、合并单元格的 Excel、双栏论文、图文混排……这些在解析阶段就把内容搞烂了,后面切得再漂亮也是在烂数据上切。

3. **RAG 是"运营"出来的,不是一次性"工程"。**
   建完库只是开始。持续补数据、修 badcase、管文档质量,系统才能活;没人维护的 RAG 会慢慢腐烂。

> **精力分配的正确顺序**:解析清洗 > 检索/重排策略 > 切分 > 生成提示词。
> 大多数人是反过来的,这就是为什么大量 RAG 项目演示惊艳、上线拉胯。

---

## 第 1 章 RAG 全链路原理(为什么演示惊艳、上线拉胯)

一条 RAG 查询从输入到输出要经过这样一条链路:

```
原始文档 → 解析(parsing) → 清洗 → 切分(chunking) → 向量化(embedding)
        → 存入向量库 → 用户提问 → 检索(retrieval) → 重排(rerank)
        → 拼接上下文 → 大模型生成(generation) → 输出
```

**关键洞察:链路上每一环的错误率会"相乘"。**

假设各环节表现如下:

| 环节 | 正确率 |
|---|---|
| 文档解析正确 | 90% |
| 切分合理 | 90% |
| 检索召回(该找到的找到了) | 85% |
| 重排后 Top-K 命中 | 90% |
| 生成不胡说 | 90% |

端到端"答对"概率 ≈ `0.9 × 0.9 × 0.85 × 0.9 × 0.9 ≈ 56%`。

这就是为什么:**任何单点看起来都还行,整体却不及格。** 要提升整体,必须每一环都抓,而不是只优化最显眼的"切分"。

---

## 第 1.5 章 文档收集阶段:怎么收、定什么标准、各格式怎么要

> 这一章发生在所有技术动作之前。**它决定了你"手里有什么原料"——原料的权威性和质量,直接封死了 RAG 的天花板。**
> 一句话:**收进来的是垃圾,后面解析、切分、检索做得再好也是"精加工垃圾"(Garbage in, garbage out)。**
> 本章全部从实际落地角度讲:谁来收、从哪收、收什么版本、定什么标准、不同格式分别怎么要。

### 1.5.1 先想清楚:收集不是"把文件都拷过来"

新手最常见的错误,是把全公司共享盘、各部门电脑里的文件**一股脑全拷进来**。结果就是:一堆重复版本、过期草稿、无关文件、私人笔记混在一起,**后面光清理就崩溃,而且很多脏数据根本清不干净**。

正确心态:**收集是"有标准的征集",不是"无差别搬运"**。你要的是——**每个业务领域里,权威的、现行有效的、高质量的那一份**,而不是它的 5 个历史版本 + 3 个人的批注版。

> **黄金原则**:**宁可少而精,不要多而杂。** 20% 的核心文档通常覆盖 80% 的真实问题,先把这 20% 收干净,远胜于把 100% 收成一锅粥。

### 1.5.2 怎么收(收集的渠道与组织方式)

**第一步:盘点"文档都散在哪"(列出来源清单)**

企业文档的典型藏身处:
- **共享盘 / NAS / 文件服务器**(最多,也最乱)
- **OA / 协同办公系统、知识库**(Confluence、Wiki、Notion、飞书/钉钉文档、SharePoint)
- **业务系统**(ERP、CRM、工单系统里能导出的文档/数据)
- **邮箱**(很多"事实上的制度"藏在邮件和附件里)
- **个人电脑**(高风险:版本最乱、最不权威)
- **纸质档案**(需要扫描)
- **官网 / 对外网页**(产品说明、FAQ)

**第二步:责任到人(最关键的落地动作)**

不要让技术团队去各部门"翻文件"——你不懂业务,分不清哪份权威。正确做法:
- **每个业务领域指定一个"文档对接人"(Document Owner)**,通常是该业务的资深员工或负责人。
- 由对接人**提供他业务域内"官方、现行有效"的文档**,并对"这是不是权威版本"负责。
- 技术团队只负责**提供收集规范 + 统一入口 + 登记**。

**第三步:统一入口,集中收集(别让文件继续散)**

- 建一个**统一的"收集区"**(一个专门的共享文件夹 / 上传入口),所有文档汇总到这里。
- 按**业务域分文件夹**存放(财务/人事/产品/法务……),对接到后面的"知识库拆分"。
- 收集区只进不改:原始文件进来后**只读保存**,任何加工都在副本上做(对应第 9 章的 `00_raw/` 只读原则)。

**第四步:边收边登记(收集登记表 = manifest 的前身)**

每收一批,就登记一行。这张表后面会直接变成第 2 章/第 9 章的 `manifest.csv`。最小字段:

| 文档名 | 业务域 | 对接人 | 来源 | 格式 | 版本/生效日期 | 是否现行有效 | 密级 | 备注 |
|---|---|---|---|---|---|---|---|---|

### 1.5.3 怎么定标准(收集验收标准 —— 收之前就发给各对接人)

**把下面这份标准做成一页"收集需求单",发给每个文档对接人,让他们照着交。** 这是落地的核心抓手,能省掉后面 80% 的扯皮和返工。

**① 权威性标准(最重要)**
- 只收**官方发布、现行有效**的版本;拒收个人草稿、笔记、未定稿、已废止文件。
- 同一文档**只交最新有效版**,不要把历史版本一起塞。

**② 版本与时效标准**
- 每份注明:**版本号、生效日期、是否仍然有效**。
- 有有效期/会更新的文档(如制度、价格表),注明**更新频率**(为后面阶段九的"文档生命周期管理"做准备)。

**③ 格式标准(详见 1.5.4)**
- **优先交可编辑原件**(.docx/.xlsx/.pptx),其次数字 PDF,最后才是扫描件/图片。
- **能给数字版就别给"打印再扫描"的版本**——这是从源头降低解析难度,比后面买任何高级 OCR 都管用。

**④ 完整性标准**
- 文件**不加密、不带打开密码、不残缺**(没有"第3页缺失"这种)。
- 附件齐全;有外链引用的,把被引用内容也一并提供。

**⑤ 命名规范(强烈建议统一)**
- 统一格式,例如:`业务域_文档名称_版本_生效日期.后缀`
- 例:`财务_差旅管理制度_v3_20240101.docx`
- 好处:文件名本身就是元数据,RAGFlow 解析时会用到,也便于去重和排序。

**⑥ 元数据随附**
- 交文档时一并填"收集登记表"那几列(业务域、对接人、密级、是否有效)。

**⑦ 密级与合规标准**
- 标注**密级**(公开/内部/机密);涉密、含个人隐私(身份证/手机/薪资)的文档**单独标记**,交由合规流程决定是否入库、是否脱敏(对应第 6 章)。

**⑧ 质量门槛(针对扫描件/图片)**
- 扫描清晰、摆正、无手指/黑边遮挡;建议**≥ 300 DPI**;水印不遮挡正文。

### 1.5.4 不同格式怎么要(从源头降低后续难度)

收集阶段最重要的格式决策是:**当同一份内容有多种格式可选时,优先要"机器最容易读"的那种。** 优先级从高到低:

```
可编辑原件(Word/Excel/PPT) > 数字PDF(可复制文字) > 扫描PDF > 图片/拍照件
       最省心 ←──────────────────────────────────→ 最难解析
```

| 格式 | 收集时怎么要(实操要点) |
|---|---|
| **Word/Excel/PPT 原件** | **首选,主动索要**。比"它导出的 PDF"好处理得多。Excel 一定要**原始 .xlsx**(保留行列结构),别要它的截图或打印版。 |
| **数字 PDF** | 可接受。索要时确认"文字能复制"(不是扫描的)。 |
| **扫描件 PDF / 图片** | **能避免就避免**:先问对接人"有没有电子原件?"。实在只有纸质,按 1.5.3 ⑧ 的扫描标准重新规范扫描,别用手机随手拍。 |
| **PPT** | 要 .pptx 原件;关键信息在图里的,提醒对接人**补一份文字说明**。 |
| **网页 / 在线文档** | 明确范围(哪些页面),交 URL 清单或导出件;注意去导航/广告噪声(第 2.5 章)。 |
| **数据库 / 业务系统数据** | 不是"文档",按需**导出成结构化文件**(Excel/CSV),走表格路线(Table 模板);并想清楚是"问答"还是"精确查询"用途。 |
| **邮件** | 只收"作为事实依据"的关键邮件;**附件单独提取**,按其本身格式处理;删层层转发引用。 |
| **音视频(如有)** | 明确要不要纳入;要的话需先转写成文字(超出多数 RAG 一期范围,建议二期再说)。 |

> **一条最实在的落地建议**:在收集需求单里写明 **"请优先提供可编辑的原始文件(Word/Excel/PPT),避免提供打印后扫描的版本"**。这一句话,能帮你从源头消灭后面最头疼的"扫描件解析"问题。

### 1.5.5 收集阶段的推进节奏(别想一次收全)

呼应全书"先小后大"原则:
1. **先选 1 个高价值、问题集中的业务域**(如:被问得最多的某部门制度)做试点收集。
2. 跑通"收集 → 登记 → 去重 → 解析验收"全流程,验证标准好不好用。
3. 再把成熟的收集规范推广到其他业务域,**分批滚动收集**,而不是全公司一次性铺开。

### 1.5.6 收集阶段 ✅ Checklist 与常见坑

**✅ 完成标准:**
- [ ] 列出了文档来源清单(共享盘/OA/邮箱/纸质…)
- [ ] 每个业务域指定了文档对接人
- [ ] "收集需求单 + 收集登记表"已下发并回收
- [ ] 文档汇总到统一只读收集区,按业务域分目录
- [ ] 每份文档有:权威版本、版本/日期、密级、对接人
- [ ] 优先拿到可编辑原件,扫描件符合质量门槛
- [ ] 先在一个业务域试点跑通

**⚠️ 常见坑:**
- 技术团队自己去翻文件,分不清权威版本 → 必须责任到业务对接人。
- 一股脑全拷,历史版本/草稿/私人笔记混入 → 只收权威现行版。
- 要了"打印扫描版"而放着 Word 原件不用 → 主动索要可编辑原件。
- Excel 收成截图/PDF → 必须原始 .xlsx。
- 收完不登记 → 后面无法去重、无法溯源、无法管版本。
- 忽略密级和隐私 → 埋下合规与泄密隐患(见第 6 章)。

> **本章一句话总纲**:**收集不是搬文件,是"按标准向业务对接人征集权威、现行、可编辑的高质量原料"。从源头优先要 Word/Excel/PPT 原件、只收最新有效版、边收边登记、先试点再铺开——这一步做对,后面解析和准确率会轻松一大截。**

---

## 第 2 章 切分前 / 中 / 后该做什么

### 2.1 切分"之前"(决定准确率上限)

这一段最该认真做,却最常被跳过。

1. **文档资产盘点**(不要把上万份当同质数据):
   - 格式分布:PDF(可复制 / 扫描)、Word、Excel、PPT、HTML、图片各占多少?
   - 结构类型:规整(合同/手册/制度)还是非结构化(邮件/聊天/新闻)?
   - 质量:多少要 OCR?多少重复/过期?
   - 价值密度:哪些是高频被问的核心知识?(通常 20% 文档覆盖 80% 问题)

2. **数据治理**(批判提醒:"全量入库"几乎总是错的):
   - 删过期、删重复、**同一文档多版本只留最新版**(否则检索打架)
   - 剔除低价值文档(空白页、签字页、封面页)

3. **文档解析(Parsing)—— 重中之重**,针对不同类型用不同武器:
   - 可复制 PDF:PyMuPDF、pdfplumber
   - 扫描件/图片:OCR(中文场景 PaddleOCR 较强)
   - 复杂版面(表格、双栏、图文混排):版面分析模型 / MinerU / 商业 API
   - 表格:**绝不能流式拼接**(会变乱码行),转 Markdown 表格或键值对句子
   - Office:python-docx、openpyxl、python-pptx,或 Unstructured

4. **清洗与规范化**:去页眉页脚水印页码、统一编码、合并被错误打断的段落、去重。

5. **设计元数据 schema**:文件名、标题、章节、页码、版本、生效日期、部门、密级。
   这些是后面做"元数据过滤"和"溯源引用"的金子。

> **验收标准**:随机抽样几十份解析后的文本,人工核对——表格有没有错位?段落顺序对不对?页眉水印有没有混进正文?**这一步省不得。**

### 2.2 切分"的时候"

**切分的本质目标**:每一块要 **语义自包含**(独立表达完整意思)且 **粒度适中**(不太大稀释语义,不太小丢上下文)。

**主流切分策略(从差到好):**

1. 固定长度切分(按 token,带 overlap):最简单,会切断句子/表格。只适合做 baseline。
2. 递归字符切分:按 段落→句子→词 递归,尽量不破坏语义边界。**工程合理默认值。**
3. 基于文档结构切分:利用 Markdown 标题、Word 标题样式、PDF 章节切。规整文档强烈推荐。
4. 语义切分:用 embedding 找"语义突变点"切。效果好但成本高、不稳定,**收益常被高估**。
5. 针对性切分:表格整块不切;QA 按问答对;代码按函数;合同按条款。

**比"选哪种算法"更值钱的实战技巧:**

- **Chunk overlap(重叠)**:相邻块重叠 10~20%,避免答案被切在边界。
- **Small-to-Big / 父子切分**:用**小块**检索(精准),命中后把**所属大块/整节**喂给模型(上下文完整)。性价比极高。
- **给每个 chunk 加"上下文头"**:块内容前拼上 `文档标题 + 章节路径`。
  例:`《差旅管理制度》>第三章 城市内交通>额度为 500 元`。检索和生成质量明显提升。
- **chunk 大小没有银弹**:中文经验区间 300~800 字(灌 md 后可 256~512),**必须用评测集调,不要拍脑袋**。

> **批判提醒**:不要陷入"切分算法军备竞赛"。干净结构化数据上,简单的"按标题切 + 父子块 + 上下文头"往往打败花哨的语义切分。

---

## 第 2.5 章 各格式原始文档"解析+清洗"傻瓜式手册(入 RAGFlow 切分之前)

> 这一章是第 2 章的"动手版":拿到一份具体格式的文档,**到底一步一步做什么**。
> 全部基于真实工程实践与公开权威基准(RAGFlow 官方文档对各格式的解析行为;IBM Research 的 DocLayNet / TableFormer 表格解析基准;Docling/Unstructured/Marker 等主流解析器的公开评测),不是空谈。
> **核心结论先说**:① 解析的难度 100% 由文档"子类型"决定(同样是 PDF,可复制的和扫描的天差地别);② **绝大多数格式不需要你手动转 Markdown**,RAGFlow 自己会解析;**只有"复杂版面扫描/双栏 PDF"这一类,才值得用外部工具(MinerU/Docling)先转成 Markdown 再进**。

### 2.5.0 通用四步法(任何格式都先走这四步)

不管什么格式,先无脑套这四步,再看下面的分格式细则:

1. **第一步:验明正身(判子类型)** —— 它是"机器能直接读的文字"还是"图片/扫描"?这决定走不走 OCR。
2. **第二步:解析(抽取文字+结构)** —— 优先让 RAGFlow 自带解析做;难件才上外部工具。
3. **第三步:清洗(去噪声)** —— 去页眉页脚水印页码、合并断行、去重、补元数据。
4. **第四步:抽样验收** —— **每类随机抽 10~20 份**,人工对照原件看解析对不对。**这一步是权威实践反复强调的铁律:解析质量决定 RAG 的天花板,必须在全量入库前用小样本验证。**

> **判断"是不是扫描件/图片型 PDF"的傻瓜方法**:用任意工具尝试"复制"PDF 里的一段文字。
> - 能选中、能复制出文字 → **数字 PDF(可复制型)**,走文字抽取。
> - 选不中、像一张图、复制出来是空的或乱码 → **扫描件/图片型**,必须走 OCR。
> - 工程化批量判断:脚本统计每页平均可提取字符数,**< 50 字/页**基本就是扫描件。

---

### 2.5.1 PDF(最常见,也最坑 —— 必须先分三种子类型)

PDF 不是一种文档,是三种,处理方式完全不同:

#### A. 数字 PDF · 单栏 · 纯文字(最简单:制度、通知、普通报告)
- **解析**:直接传 RAGFlow,选对应模板(制度/手册用 **Manual**,普通文章用 **General**)。RAGFlow 的 DeepDoc 直接搞定。
- **清洗**:开启"去页眉页脚 / 去目录"(RAGFlow 解析器有 *Remove original table of contents* 等选项);确认没有水印混进正文。
- **要不要转 Markdown?** **不要**。多此一举还可能引入错误。
- **验收**:抽查 chunk,看段落顺序、标题层级对不对。

#### B. 复杂版面 PDF(双栏论文 / 图文混排 / 带公式 / 跨页大表)—— 唯一真正值得转 Markdown 的一类
- **难点(权威结论)**:**表格是公认最难解析的元素**,合并单元格、跨页表格、多级表头会让"朴素抽取"直接崩;双栏的阅读顺序也容易乱(把左右栏串成一行)。
- **解析(傻瓜步骤)**:
  1. 先试 RAGFlow 把**解析后端切成 MinerU**(RAGFlow 已内置),用 Paper/Manual 模板,抽样看效果。
  2. 若仍不满意 → 走**外部 MinerU / Docling 转成 Markdown**(公式→LaTeX、表格→HTML/Markdown 表格、按阅读顺序重排),再以 md 进 RAGFlow 走 General+按标题。
     - 参考:在公开的 DocLayNet 表格基准上,带版面模型的解析器(如 Docling 的 TableFormer)表格结构识别可达 ~95%+,远高于无版面分析的朴素抽取。**这正是这类难件值得上重型工具的原因。**
- **清洗**:重点检查**表格有没有错列、公式对不对、双栏顺序对不对**。
- **要不要转 Markdown?** **是,这是唯一强烈推荐外部转 md 的场景**(前提是 RAGFlow 内置 MinerU 仍搞不定,或你需要做批量脚本清洗 / 数据资产复用,见第 8 章)。
- **验收**:逐份核对抽样里的表格和公式,合格率 ≥ 90% 才放行。

#### C. 扫描件 / 图片型 PDF(老合同、盖章件、拍照件)
- **难点**:本质是图片,必须 OCR;中文 + 复杂版面尤其难。
- **解析(傻瓜步骤)**:
  1. 优先用 **MinerU(自带 OCR + 版面分析,109 语言)** 解析,或 RAGFlow 内置 MinerU 后端。
  2. 中文纯文字扫描件,也可用 **PaddleOCR** 类工具。
  3. 输出统一成文字 / Markdown。
- **清洗**:OCR 必有错字,**乱码率要卡**(建议 < 2%,超了回炉);去掉印章、签名等噪声块。
- **要不要转 Markdown?** **通常要**(因为必须经过 OCR/MinerU 这一道,顺便产出 md)。
- **验收**:抽样核对 OCR 文字准确率,这是这类文档的命门。

> **PDF 一句话总纲**:先分三类——A 类直接进 RAGFlow;C 类必须 OCR;**只有 B 类(复杂版面)才是"先转 Markdown"的主战场**。别把简单 PDF 也拿去转 md。

---

### 2.5.2 Word(DOCX)

- **特点**:本身就是结构化的(有标题样式、段落、表格),通常解析很顺,是"省心格式"。
- **解析**:直接传 RAGFlow。制度/手册类用 **Manual**(新版支持 DOCX),普通文档用 **General**;长篇用 **Book**。
- **清洗**:
  - 确认作者用了**标题样式**(标题1/标题2)而不是"手动调大加粗"——前者 RAGFlow 能识别层级,后者识别不了。若文档是"假标题",考虑预处理修正。
  - 删修订痕迹、批注、文本框里的零散内容。
  - 文档内的表格:简单表 RAGFlow 能处理;若是复杂大表,考虑单独抽出来按表格处理。
- **要不要转 Markdown?** **不要**。DOCX 直接解析通常就够好,转 md 反而可能丢标题层级。
- **验收**:抽查标题层级 `#`/`##` 是否正确。

---

### 2.5.3 Excel / CSV(表格数据 —— RAGFlow 有专门规则,务必看懂)

这是最容易"静默出错"的格式(不报错,但表格变乱码)。**RAGFlow 对表格有明确的官方行为,必须按规则来:**

**先判断你的表格是哪一种用途:**

#### A. 当成"知识/内容"被问答(如:产品参数表、制度附表)
- **用 General 模板**,然后**关键操作:打开 `Excel to HTML` 开关**(官方功能)。
- **官方明确规则(重要)**:
  - 默认**不开**时,表格被转成"键值对"。简单表勉强可以;
  - **复杂表(多列、合并单元格、一个 sheet 多张表)如果不开 Excel2HTML,RAGFlow 不会报错,但表格大概率变成乱码!** —— 这是官方原文警告。
  - 开启后,表格转成 HTML 表格保留结构;**超过 12 行的表会自动按每 12 行切一块**。
- **清洗**:合并单元格尽量在源文件里拆开填充;删空行空列;每张表加个清楚的表头/标题。

#### B. 当成"结构化数据库"做精确查询(如:从数据库导出的台账、清单)
- **用 Table 模板**。官方机制:**Table 模板会把用户问题转成 ES SQL 去查**,适合"从数据库 dump 出来的结构化数据",让人不用写 SQL 就能查。
- 这类**不要**当成普通文本切。

#### C. CSV
- 同 Excel,按上面 A/B 选 General(简单)或 Table(结构化查询)。

- **要不要转 Markdown?** **强烈不要!** 这是新手最常犯的错:把 Excel 压成 Markdown 表格,**反而丢掉了 RAGFlow 的 Excel2HTML / Table(TSI/SQL)这些原生结构化能力**,得不偿失。**Excel 保持原样进 RAGFlow,用对开关和模板。**
- **验收**:拿几个"针对表格的问题"实测,答得不对就回去检查 Excel2HTML 开关。

---

### 2.5.4 PPT(PPTX / PPT)

- **特点**:信息是"空间排布"的(标题、要点、图表散在各处),阅读顺序天然零散,是有难度的格式。
- **解析**:直接传 RAGFlow,用 **Presentation 模板**(**按每一页幻灯片切**,支持图文)。
- **清洗**:
  - 幻灯片里的**关键图表/示意图**,文字解析抓不到含义 → 重要的图考虑人工补一句文字说明,或用多模态模型生成图说明再并入。
  - 删母版页、版权页、过渡页等无信息页。
- **要不要转 Markdown?** **不要**,Presentation 模板就是为 PPT 设计的。
- **验收**:抽查每页 chunk 是否完整、要点有没有丢。

---

### 2.5.5 图片(JPG / PNG / TIF / GIF)

- **特点**:纯图,内容全靠 OCR / 多模态理解。
- **解析**:
  - 图里是**文字**(截图、扫描页)→ 用 RAGFlow **Picture 模板**(走 OCR),或先用 MinerU/PaddleOCR。
  - 图里是**图表/照片**(没文字或文字少)→ OCR 抓不到含义,需**多模态模型生成描述**再入库。
- **清洗**:提升清晰度(去模糊/摆正/去噪),OCR 错字检查。
- **要不要转 Markdown?** 经过 OCR 这步,**通常会产出文字/md**,看工具而定。
- **验收**:OCR 文字准确率抽查。

---

### 2.5.6 HTML / 网页

- **特点**:含大量导航栏、广告、页脚、脚本等**噪声**,正文淹没其中。
- **解析**:直接传 RAGFlow(支持 HTML,General 模板);若是抓取的网页,**清洗比解析更重要**。
- **清洗(重点)**:
  - 用正文提取(如 readability 类算法)**剥离导航/广告/页脚**,只留正文。
  - 去掉残留的 HTML 标签碎片、JS、CSS。
- **要不要转 Markdown?** **可选且常见**:网页转干净 Markdown 是成熟做法(去掉噪声、保留标题/列表/链接结构),对大批量爬取内容尤其值得。少量规整网页直接进 RAGFlow 也行。
- **验收**:抽查正文有没有被噪声污染。

---

### 2.5.7 TXT / Markdown(已是纯文本)

- **特点**:最干净,几乎不用解析。
- **解析**:直接传 RAGFlow,用 **General**;Markdown 有标题结构的,**开"按标题切"** 效果最好。
- **清洗**:统一编码(防中文乱码)、合并异常断行。
- **要不要转 Markdown?** 本来就是文本,**无需转换**(md 直接用最佳)。
- **验收**:抽查切块边界是否合理。

---

### 2.5.8 邮件(EML)

- **特点**:有结构(主题/发件人/正文/附件/引用层层嵌套)。
- **解析**:用 **Email 模板**,按邮件结构处理。
- **清洗**:
  - 删除"---原始邮件---"层层转发的重复引用(否则大量重复内容污染检索)。
  - **附件要单独抽出来**,按其各自格式(PDF/Word/Excel)走前面对应流程。
  - 删邮件签名、免责声明长尾。
- **要不要转 Markdown?** **不要**,用 Email 模板;附件按附件格式处理。
- **验收**:正文干净、附件被正确分流。

---

### 2.5.9 JSON / 结构化日志等

- **特点**:本身是结构化数据。
- **解析**:简单 JSON 可用 General;若是大量记录的结构化数据,考虑当表格/数据库思路(Table 模板或先转成"一条记录一句话")。
- **清洗**:把嵌套 JSON 拍平成可读句子,或保留为结构化字段。
- **要不要转 Markdown?** 一般不需要;视用途转成"自然语言句子"往往比硬切 JSON 更好检索。

---

### 2.5.10 一页纸总表:格式 → 怎么做 → 要不要转 Markdown

| 原始格式 | 子类型 | 解析怎么做 | 清洗重点 | 进 RAGFlow 用什么 | **要不要转 Markdown** |
|---|---|---|---|---|---|
| PDF | 数字·单栏纯文字 | RAGFlow 直接解析 | 去页眉页脚/目录 | Manual / General | **否** |
| PDF | 复杂版面(双栏/公式/大表) | 内置 MinerU;不行则外部 MinerU/Docling | 表格/公式/阅读顺序 | md→General+按标题,或 Paper | **是(唯一主战场)** |
| PDF | 扫描件/图片型 | MinerU/PaddleOCR 走 OCR | 乱码率<2%,去印章 | md→General,或 Picture | **通常是(因走 OCR)** |
| Word DOCX | — | RAGFlow 直接解析 | 确认真标题样式 | Manual / General / Book | **否** |
| Excel/CSV | 当内容问答 | RAGFlow,**开 Excel2HTML** | 拆合并单元格、删空行 | **General + Excel2HTML** | **否(强烈不要)** |
| Excel/CSV | 当数据库查询 | RAGFlow Table 模板 | 规整成标准表 | **Table(转 SQL 查)** | **否** |
| PPT | — | RAGFlow 直接解析 | 补图表说明、删废页 | Presentation | **否** |
| 图片 | 含文字 | OCR(Picture/MinerU) | OCR 错字、清晰度 | Picture | 通常经 OCR 产出 |
| 图片 | 图表/照片 | 多模态模型生成描述 | 描述准确性 | 描述文本入库 | — |
| HTML/网页 | — | RAGFlow 或先抽正文 | 剥导航/广告/页脚 | General | **可选(批量爬取建议转)** |
| TXT/MD | — | 直接用 | 统一编码 | General(md 开按标题) | **否(本就是文本)** |
| 邮件 EML | — | Email 模板 | 删转发引用、拆附件 | Email | **否** |
| JSON | — | 按用途 | 拍平成可读句子 | General / Table | 一般否 |

### 2.5.11 "要不要转 Markdown"的傻瓜判断口诀

记住一句话就够:**默认都不转,RAGFlow 自己会解析;只有当文档"是复杂版面/扫描的 PDF、或大批量噪声网页、或你要做脚本化批量清洗与数据资产复用"时,才转 Markdown。** 尤其 **Excel 和 FAQ 千万别转**——转了会丢掉 RAGFlow 的结构化能力(详见第 6、8 章)。

> **本章铁律(权威实践共识)**:无论选哪条路,**全量入库前,每类格式都先拿 10~20 份做解析抽样验收**。解析对不对,决定了你后面所有努力的上限——这一步省下的时间,会在排查准确率问题时加倍还回去。

---

## 第 3 章 检索与生成:决定准确率的另一半

切分只是把数据准备好,**真正决定"能不能找到对的内容"的是检索策略**。这部分被绝大多数人忽视。

### 3.1 检索阶段

1. **混合检索(Hybrid Search)= 向量检索 + 关键词检索(BM25)**
   向量擅长语义近似,但对**专有名词、型号、编号、人名**很差;关键词检索正好补上。
   **生产环境几乎必上,最高性价比的准确率提升手段之一。**

2. **重排(Rerank)**
   向量召回 Top 20~50,再用 rerank 模型(BGE-reranker、Cohere Rerank)精排出 Top 3~5。
   **通常带来肉眼可见的提升,强烈建议必上。**

3. **查询改写 / 扩展(Query Rewriting / HyDE)**
   口语化、有歧义的问题先用 LLM 改写规范,或生成"假设答案"再检索。

4. **查询路由(Routing)**
   多库情况下,判断用户问题该去哪个知识库查。

5. **元数据过滤**
   用户问"2024 年报销标准",先按 `year=2024 + type=财务` 过滤再检索。

### 3.2 生成阶段(直接决定准确率和信任)

- **提示词约束**:强制"只根据检索内容回答、不许编造、必须给出处"——幻觉控制的关键。
- **溯源 / 引用(Citation)**:答案必须能点回原文哪一段。**企业用户不信任没出处的 AI。**
- **拒答策略**:检索不到就答"未在知识库中找到",**绝不硬编**。
  > "宁可不答,不可乱答"是高准确率的前提。

---

## 第 3.5 章 Embedding 与 Rerank 模型选型:文本还是多模态

> 检索靠两个模型:**Embedding(把文字/图片变成向量,负责"召回")** 和 **Rerank(把召回的结果精排,负责"排得准")**。
> 这两个模型各有"文本版"和"多模态版",到底怎么选?本章基于公开权威基准(ViDoRe 视觉文档检索基准、相关学术论文)和 RAGFlow 官方实际行为来讲。

### 3.5.1 先破一个常见误区:用了多模态就能"随便收集"吗?

**不能。** 很多人以为"多模态模型能直接看懂图片/扫描件,那原始文档随便丢进去就行",这有两个硬伤:

1. **数据治理和用什么模型无关,治理永远省不掉。**
   去重、只留最新有效版、权限、密级、命名规范……这些和你用文本还是多模态 embedding **完全无关**。多模态再强,把 3 个过期版本 + 草稿一起灌进去,照样检索打架、照样泄密。**"Garbage in, garbage out" 对多模态一样成立。**

2. **多模态"省解析"是有代价的,不是免费午餐。**
   多模态(尤其 page-as-image 路线)能跳过 OCR,但代价很重:**一页 PDF 会产生 1000+ 个向量**(ColPali 每页约 1030 个),是单向量文本的 100~1000 倍;**存储和算力暴涨、延迟变高,且需要专门支持多向量(MaxSim)的向量库**(Qdrant/Vespa 等)。

> **结论**:多模态能减轻"复杂版面/图表"的解析负担,但它**替代不了数据治理**,而且把成本压力从"解析"转移到了"存储和检索"。**第 1.5 章的收集标准一条都不能少。**

### 3.5.2 文本 vs 多模态:到底哪个好?(看信息长在哪)

**没有绝对好坏,取决于"你的关键信息长在哪里"。** 这是有基准支撑的结论:

| 你的文档主要是 | 推荐 | 依据 |
|---|---|---|
| **文字为主**(制度、合同、手册、FAQ、普通报告) | **文本 Embedding + 文本 Rerank** | 在多数企业语料上,单向量文本模型(BGE-M3、Voyage、OpenAI)已能与多模态打平,而**存储成本只是零头**,延迟更低、最成熟 |
| **图表/扫描/版式为主**(财报、PPT 幻灯片、技术图纸、扫描件、密集表格) | **多模态/视觉 Embedding**(ColPali、ColQwen2、Jina v4 等) | 公开的 ViDoRe 视觉文档基准上,ColPali 比"OCR+切分+BGE"文本方案高约 **15 个 nDCG@5 点**(0.81 vs 0.66);一篇财报问答论文显示多模态检索 mAP 相对提升约 **32%** |

**关键判断:**
- **文字型文档,文本模型不仅够用,而且是更优解**(更准、更便宜、更快)。别被"多模态更高级"带偏。
- **只有当关键信息真在图表、版式、扫描页里,且 OCR 抠不回来时**,多模态才显著胜出。
- **业界主流落地是"混合 / 路由"**:默认走文本,只把"视觉上很难、文本检索没信心"的页面路由给多模态,兼顾准确率和成本。

### 3.5.3 选型速查表(按文档/查询特征)

| 场景特征 | Embedding 选择 |
|---|---|
| 短 FAQ、干净文本 | 单向量文本(BGE / OpenAI / Voyage) |
| 长 PDF、混合版式但文字可抽取 | 文本(必要时多向量文本 ColBERT 类) |
| 图表、表格、扫描件、幻灯片为主 | 多模态 / 视觉(ColPali / ColQwen2) |
| 延迟敏感、高并发 | 单向量文本 + Rerank |
| 追求极致召回、愿付存储成本 | 多模态 + 多向量后期交互(late interaction) |

> **对你的场景(企业上万份制度/手册类、文字为主的文档)**:**文本 Embedding + 文本 Rerank 就是正确选择**。中文建议 **BGE 系列 embedding + BGE-reranker**。不需要、也不建议一上来就上多模态——那是给"图表/扫描密集"场景准备的重武器。

### 3.5.4 RAGFlow 里是文本还是多模态?

**RAGFlow 的核心检索路线是"文本为主",处理图片靠"先转成文字":**

1. **Embedding**:默认/集成的是**文本**模型(BGE、BCE、Jina);也支持接入多模态(如 Voyage Multimodal 3),但**不是开箱即用的 page-as-image 视觉检索**。
2. **Rerank**:是**文本** rerank;且**新版已移除内置 rerank**(官方理由:对召回率提升小、却明显增加检索时间),改为**接外部 rerank**(vLLM / Xinference / HuggingFace / 各家 API)。
3. **图片怎么处理**:走 **"caption-and-index"** 路线——用 **VLM 多模态模型把 PDF/DOCX 里的图片、扫描内容理解成文字描述**(DeepDoc + VLM pipeline,2025-03 起),然后**仍然用文本检索**。
4. **真正的视觉多模态检索(ColPali/Jina v4 多向量)**:RAGFlow **非默认支持**,需自己通过"可配置 embedding 面板"接入,并配多向量向量库,属进阶定制。

> **一句话**:RAGFlow 本质是 **"文本检索引擎 + 用多模态模型把图变成文字"**,而不是"纯视觉多模态检索引擎"。对文字为主的企业文档,这个路线刚好够用且最省心。

### 3.5.5 落地建议(给文字为主的企业文档)

1. **Embedding**:选成熟中文文本模型(BGE 系列);**选定后不可改,改了要全库重灌**,务必先定。
2. **Rerank**:接一个外部文本 rerank(如 BGE-reranker),**强烈建议开**,性价比高。
3. **图片/扫描内容**:用 RAGFlow 的 VLM 图片理解(转文字)即可,不必上视觉多模态检索。
4. **何时才考虑多模态**:当评测发现"大量答错都来自图表/扫描页、且 OCR/VLM 转文字也救不回来"时,再针对这部分做"路由到多模态"的进阶方案——**用评测数据驱动决策,不要拍脑袋上多模态**。

> **本章铁律**:**模型选型要被"文档类型 + 评测数据"驱动,而不是被"听起来高级"驱动。** 文字为主就用文本模型,把省下的预算花在数据治理和评测集上,回报率高得多。

---

## 第 4 章 "95% 准确率"怎么定义和验证

### 4.1 先把"准确率"拆开(严谨性的关键)

单一的 95% 是伪命题。必须分层度量:

**A. 检索质量(只看找得准不准,不看大模型):**
- 命中率 / Recall@K:正确答案所在 chunk 有没有进 Top-K
- MRR / NDCG:正确内容排得够不够靠前

**B. 生成质量(给定检索内容,答得对不对):**
- 忠实度 / Faithfulness:有没有忠于检索内容(不胡编)
- 答案相关性:有没有答到点上
- 正确性:与标准答案比对

> **现实校准**:端到端 95% 在开放问答里非常高、多数项目达不到。但封闭域、问题集中、文档质量高(如固定 FAQ、制度问答),配合"答不出就拒答",做到高准确率是可能的。**一定要和需求方对齐:95% 指检索命中率还是端到端答对率?口径不同,难度差一个数量级。**

### 4.2 怎么验证(必须建评测集,工程化分水岭)

1. **构建黄金评测集(Golden Set)**:从真实业务收集 100~300 个代表性问题,每个标注:① 标准答案 ② 答案出处。**这是整个项目最有价值的资产,没有之一。**
2. **自动化评测**:用 RAGAS / TruLens / DeepEval 等跑指标。可用强模型做 LLM-as-judge,但要抽样人工校验裁判本身是否靠谱。
3. **分层归因**(出错时定位问题在哪)——答错先看 Top-K 里有没有正确 chunk:
   - **没有 → 召回问题**(查解析/切分/检索)
   - **有但答错 → 生成问题**(查重排/提示词/生成)
4. **回归测试**:每次改动用评测集重跑,防止"修好一个、弄坏三个"。
5. **持续迭代**:上线后收集真实问题和踩的 case,不断回灌评测集。

---

## 第 5 章 产品选型:扣子 / Dify / RAGFlow 原理与对比

**共同底层原理**:三者都是把"解析→切分→向量化→检索→生成"链路产品化、可视化了。区别在侧重点和可控程度。

### 5.1 扣子(Coze,字节)
- **定位**:面向大众的 AI Bot / Agent 搭建平台,知识库是其中一个模块。
- **特点**:极易上手,可视化拖拽,生态强,适合做对话机器人、轻量应用。
- **批判**:**可控性和透明度最低**,切分/检索是黑盒,出问题难精细调优;数据要上传平台,**合规/私有化是硬约束**。适合 demo、个人、轻量 C 端,**不适合上万份复杂文档 + 高准确率的严肃场景**。

### 5.2 Dify(开源,可私有化)
- **定位**:LLM 应用开发平台(LLMOps),知识库 + 工作流编排 + Agent。
- **特点**:可视化工作流、支持混合检索/rerank、模型可换、**可私有化**,工程化和可控性远强于扣子。
- **批判**:**文档解析是相对短板**,对复杂版面/表格/扫描件不如 RAGFlow。强项是"应用编排"而非"文档理解"。适合要做完整 AI 应用、把 RAG 嵌进更大业务流的团队。

### 5.3 RAGFlow(开源,InfiniFlow)
- **定位**:**专注 RAG、专注"文档深度理解"** 的引擎。
- **特点**:核心是 **DeepDoc 深度文档解析**(版面分析、表格识别、OCR),针对复杂格式做了大量工作;提供"模板化切分"、可视化引用溯源;新版**已内置集成 MinerU / Docling 作为解析后端**。
- **批判**:正好补 Dify/扣子的短板(解析强);但应用编排、生态不如 Dify;部署相对重。**适合"上万份各种格式文档解析不好"这个痛点。**

### 5.4 选型建议(针对"上万份、多格式、高准确率")
- **数据敏感 / 要私有化 + 文档格式复杂** → **RAGFlow 做知识库底座**。
- **还要复杂业务编排、多步 Agent** → **Dify**,或 Dify 编排 + 更强解析方案补足。
- **扣子** → 仅做快速验证或轻量场景。
- **务实做法**:先用 RAGFlow / Dify 快速搭 baseline + 建评测集,用数据说话,再决定是否自建。

> **客观提醒**:这些产品迭代极快,任何二手结论都会过时。**用你自己的评测集、在你自己的数据上各跑一遍**,才是独立审查者该有的态度。

---

## 第 6 章 RAGFlow 分块方法详解(最新版)

> 截至 2026 年 6 月,RAGFlow 最新稳定版为 **v0.25.6**(2026-05-26 发布)。
> 核心设计:**不让你自己写切分规则,而是按"文档类型"提供预置模板**,每个模板封装了"用什么解析器 + 怎么识别版面 + 怎么切"。

### 6.1 内置分块模板一览

| 模板 | API 名 | 大白话解释 | 官方支持格式 |
|---|---|---|---|
| **General(通用)** | `naive` | 默认万能款。按设定 token 数(默认 512)连续切,尽量在段落/换行处断开。啥都能塞,但都不是最优。 | MD, DOCX, XLSX, PPT, PDF, TXT, 图片, CSV, JSON, EML, HTML 等几乎全格式 |
| **Q&A(问答)** | `qa` | 专门吃问答对,把"问题"和"答案"绑成一块。做 FAQ/客服库神器。 | XLSX, CSV/TXT(新版也支持 DOCX/MD/PDF) |
| **Resume(简历)** | `resume` | 专解析简历,抽取姓名/学历/工作经历。**仅企业版**。 | DOCX, PDF, TXT |
| **Manual(手册)** | `manual` | 针对操作手册/说明书这类有清晰章节层级的文档,按章节切。 | PDF(新版加了 DOCX) |
| **Table(表格)** | `table` | 专吃纯表格文件,用 TSI 技术理解行列关系,把表当结构化数据解析。 | XLSX, CSV/TXT |
| **Paper(论文)** | `paper` | 针对学术论文,按摘要、章节、参考文献结构切。 | PDF |
| **Book(书籍)** | `book` | 针对长篇书籍,按目录/章节切。 | DOCX, PDF, TXT |
| **Laws(法律)** | `laws` | 针对法律法规/合同,按"条/款/章"切,保证一条法条不被切散。 | DOCX, PDF, TXT |
| **Presentation(演示)** | `presentation` | 针对 PPT,按"每一页幻灯片"切,支持图文。 | PDF, PPTX |
| **Picture(图片)** | `picture` | 针对图片文件,走 OCR 抠文字。 | JPEG, PNG, TIF, GIF 等 |
| **One(整文档)** | `one` | 整份文档当成一个块,完全不切。适合很短或需全文一起喂模型的文档。 | DOCX, XLSX, PDF, TXT |
| **Email(邮件)** | `email` | 针对邮件,按邮件结构处理。 | EML |
| **Tag(标签)** | `tag` | 特殊用途:这个库当成"标签词典",给其他库的 chunk 自动打标签辅助检索。 | XLSX, CSV/TXT |

### 6.2 两个重要的检索增强能力(叠加在模板之上)

- **RAPTOR(含新版 Ψ-RAG / AHC 模式)**:把 chunk 自动聚类、逐层生成摘要,形成"知识树"。
  适合回答需要**跨段落、跨文档归纳总结**的问题。新版把语义聚合扩展到**整个数据集级别**。
  **代价**:建索引慢、耗 token,按需开。
- **Knowledge Graph / GraphRAG**:从文档抽取实体和关系建图,适合**关联推理**问题。
  **代价更大**,不要无脑全开。

### 6.3 可编排的 Ingestion Pipeline(新)

从 v0.24 起提供可视化流水线:`Parser → Transformer(加摘要/关键词)→ Chunker → Indexer`。
Chunker 支持两种核心方式(可串联):
- **By Tokens(按 token)**:默认 512,可设重叠和分隔符。
- **By Title(按标题/层级)**:按章节切,每块是完整结构单元——适合手册、论文、合同。
- 支持接入 **MinerU / Docling** 解析后端,以及**父子分块(parent-child)**。

### 6.4 哪种文件用哪种分块方法(速查)

> 原则:**能用专用模板就别用 General。**

| 你的文档类型 | 推荐模板 | 理由 |
|---|---|---|
| 规章制度、操作手册、说明书 | **Manual** | 按章节切,保条理 |
| 法律法规、合同、条文 | **Laws** | 按"条/款"切,法条不被切散 |
| 学术论文、研报 | **Paper** | 按论文结构切 |
| 长篇书籍、长文档 | **Book** | 按目录章节切 |
| FAQ、客服问答 | **Q&A** | 问答绑定,命中最准 |
| 纯数据表(Excel/CSV) | **Table** | TSI 理解行列,别用 General |
| PPT 演示文稿 | **Presentation** | 按页切,保图文 |
| 图片、扫描件(图片型) | **Picture** + 强 OCR | 自带 OCR 不够好就外部预处理 |
| 邮件 | **Email** | 按邮件结构 |
| 简历 | **Resume**(企业版) | 抽结构化字段 |
| 很短/需整体理解 | **One** | 整篇当一块 |
| 普通文章、md、网页、杂项 | **General** | 兜底默认,调好参数 |
| 需跨文档总结 | 任意模板 + **RAPTOR** | 代价:慢、耗 token |
| 需实体关系推理 | 任意模板 + **GraphRAG** | 代价更大,谨慎 |

> **关键提醒**:模板名字很"贴心",但别被骗了。模板只是"预设参数 + 预设解析器"的打包,**效果完全取决于第一步——文档能不能被正确解析**。真正的功夫在"放进 RAGFlow 之前"。

---

## 第 7 章 解析工具:MarkItDown vs MinerU 深度对比

**一句话定性**:这俩不是竞品,是两个层级的东西。
**MarkItDown = 广度优先的"格式转换器"**(轻量、什么都能转、深度浅);
**MinerU = 深度优先的"文档解析引擎"**(靠 AI 模型死磕复杂 PDF 的版面/公式/表格,精度高、但重)。

### 7.1 基本信息(截至 2026 年 6 月)

| 维度 | MarkItDown | MinerU |
|---|---|---|
| 所属 | Microsoft | OpenDataLab(源自 InternLM) |
| 仓库 | `microsoft/markitdown` | `opendatalab/MinerU` |
| Star | ~148K | ~67K |
| 最新版本 | v0.1.6(2026-05) | 3.2.3(2026-06) |
| **许可证** | **MIT(宽松,商用无忧)** | **较严格 copyleft(AGPL 系,商用务必核实)** |
| 定位 | 各种文件转 Markdown 喂 LLM | 高精度文档解析引擎 |

> **许可证是企业最该先看的**:MarkItDown(MIT)随便商用;MinerU(AGPL 系)若做成对外网络服务可能触发开源义务,**务必让法务确认**。

### 7.2 核心定位差异

- **MarkItDown**:调度各种现成解析库的统一封装,**广度、简单、纯 CPU、秒级**。官方明说"不适合高保真场景"。它不"理解"版面;要高保真需接 Azure(数据出门)。
- **MinerU**:自带深度学习模型的流水线,**VLM + OCR 双引擎**。版面分析、按阅读顺序重排、公式转 LaTeX、表格转 HTML、自动去页眉页脚。**代价:重,最佳性能需 GPU。**

### 7.3 能力对比

| 能力 | MarkItDown | MinerU |
|---|---|---|
| 输入格式广度 | **极广**(含音频转写、ZIP、YouTube URL) | 较窄但精(PDF、图片、DOCX、PPTX、XLSX) |
| 复杂 PDF 版面(双栏) | 弱,易乱序 | **强项**,按阅读顺序重建 |
| 扫描件 / 手写 OCR | 基础 | **强项**,109 语言 |
| 数学公式 | 基本不行 | **自动转 LaTeX** |
| 表格 | 基础 | 转 HTML,**支持跨页合并** |
| 去页眉页脚 | 基本不处理 | **自动去除** |
| 运行要求 | **纯 CPU,轻量,快** | **建议 GPU**,重,慢 |
| 上手 | `pip install markitdown` 一行 | 较重,要下模型 |
| 生态 | AutoGen/LangChain,插件 | MCP、LangChain/Dify/FastGPT 原生集成 |

### 7.4 选型结论(上万份多格式)

**不要二选一,按难度分流组合用:**
- **格式杂、内容简单**(普通 Office/HTML/CSV/md)→ **MarkItDown**(轻、快、广,纯 CPU)
- **解析难的硬骨头**(扫描件、双栏、公式、复杂表格)→ **MinerU**(上 GPU 砸精度)
- 都统一产出干净 Markdown,再进 RAGFlow(RAGFlow 已原生支持 MinerU/Docling)

---

## 第 8 章 关键决策:到底要不要先转 Markdown?

这是最容易想错的一步。**结论:大多数情况下,不需要把上万份全部先转成 Markdown 再灌。**

### 8.1 关键事实:RAGFlow 已内置 MinerU

RAGFlow 新版**已内置集成 MinerU(和 Docling)作为解析后端**。
这意味着:你想要"MinerU 的解析质量",**并不等于"必须自己在外面搭一条 MinerU→Markdown 流水线"**——直接把原始文档上传 RAGFlow、把解析后端选成 MinerU 即可。

### 8.2 三条路线

| 路线 | 怎么做 | 适合谁 |
|---|---|---|
| **A. 原文档 + RAGFlow 自带模板** | 原始文档直接传,选 Manual/Laws/Table 等 | **大多数规整文档**。最简单 |
| **B. 原文档 + RAGFlow 内置 MinerU 后端** | 原始文档传,难件解析后端切 MinerU | **解析难的件,又不想自建流水线。多数人的最优解** |
| **C. 外部 MinerU→Markdown→再灌** | 自建流水线先转 md 再入库 | **只有满足下面特定理由时才值得** |

> **绝大多数情况走 A + B 组合就够了,不需要 C。**

### 8.3 外部转 Markdown(路线 C)什么时候才真有必要

它的价值**不在解析质量**(RAGFlow 内置 MinerU 就能给),而在这些特定场景:

1. **要做大规模程序化清洗 / 质检**:用自己脚本批量去水印、合并版本、统一标题、加元数据、修表格。在 md 文件上用脚本搞,比在 RAGFlow 界面里搞顺手。
2. **要把"干净数据"做成可复用资产、不锁定 RAGFlow**:解析很贵,一份干净 md 是可移植、一次解析到处用的资产(以后换 Dify/自建也能用)。
3. **要把重型解析和灌库在算力/时间上解耦**:让 GPU 密集的 MinerU 作为独立离线批处理,不拖慢 RAGFlow 在线解析。
4. **需要超出 RAGFlow 暴露范围的 MinerU 精细控制**:锁版本、调细参、保留 JSON 中间产物。

**以上一条都不强烈需要,就别搭路线 C,纯属加维护负担。**

### 8.4 反过来:有些文档转 Markdown 反而帮倒忙

- **纯表格 Excel/CSV** → 用 RAGFlow 的 **Table 模板(TSI)**;压成 md 表格反而丢行列结构。
- **FAQ / 问答对** → 用 **Q&A 模板**;压成 md 流式文本破坏问答结构。
- **规整 Word/PPT** → DeepDoc 直接解析通常就够,转 md 多此一举还可能引入错误。
- **双重解析风险**:转了 md 又在 RAGFlow 误选 PDF/Manual 解析 = 解析两次,易出错。(md 进去只能走 General。)

---

## 第 9 章 MinerU → Markdown → RAGFlow 落地标准

> 适用于**确实需要走外部转换**(路线 C)的那部分难件。核心原则仍是"分流 + 质量门禁 + 评测验收"。

### 9.1 目录结构标准(强制统一)

```
/rag_project
├── 00_raw/                  原始文档,只读,永不修改
├── 01_inventory/            盘点清单 manifest.csv
├── 02_markdown/             转换后的 md,镜像 00_raw 结构
├── 03_markdown_clean/       清洗后、待灌库的 md
├── 04_rejected/             质检不合格、需人工处理
├── 05_eval/                 黄金评测集 + 评测脚本
└── logs/                    转换日志、失败清单
```

### 9.2 盘点清单 `manifest.csv` 字段标准

| 字段 | 说明 |
|---|---|
| file_id | 唯一 ID(内容 hash) |
| path | 原始路径 |
| format | 格式 |
| doc_class | 业务分类 |
| parse_route | 分流标记(mineru / markitdown / table) |
| is_scanned | 是否扫描件 |
| version / year | 版本 / 年份 |
| dept / 密级 | 元数据 |
| status | pending / done / rejected |

> **判断 is_scanned**:检测 PDF 每页平均可提取字符数,过低(经验值 < 50 字/页)判为扫描件,走 MinerU。

### 9.3 MinerU 转换执行标准

- **硬件**:GPU 必备(纯 CPU 吞吐太低)。VLM 模式建议 ≥8GB 显存、32GB 内存。
- **环境**:Python 3.10–3.13,锁定 MinerU 版本(可复现)。
- **批量**:按 doc_class 分目录,每批 500~1000 份;失败进 `logs/failed_list.txt` 重试;脚本支持**断点续跑**。
- **保留 JSON 中间产物**(含版面坐标、阅读顺序),便于定位和二次处理。
- **公式→LaTeX、表格→HTML、自动去页眉页脚**:开启。

### 9.4 Markdown 质量门禁(核心标准,决定准确率命门)

**A. 自动化质检(不达标进 `04_rejected/`):**

| 检查项 | 不合格标准(示例阈值) |
|---|---|
| 空文件/近空 | 正文 < 50 字且原文非空 → 拒 |
| 乱码率 | 乱码字符 > 2% → 拒 |
| 文本提取率 | md 字数 / 原文估算字数 < 70% → 拒 |
| 表格完整性 | 原文有表格但 md 无 table 标记 → 拒/复查 |
| 页眉页脚残留 | 重复页码/水印行未清干净 |

**B. 人工抽样质检(自动测不出语义错位):**
- 每个 doc_class 随机抽 **5%(每类至少 10 份、封顶 50 份)**,对照原件核对表格错位、公式、标题层级、多栏顺序。
- **验收门槛:抽样合格率 ≥ 90% 才放行**该类批量入库。

**C. 清洗 + 加 YAML front matter 元数据:**

```yaml
---
title: 差旅管理制度
doc_class: 财务制度
year: 2024
dept: 财务部
source_file: 差旅2024.pdf
version: v3
---
```

> **硬规矩**:宁可拒一批回炉,也不要把烂 Markdown 灌进库。脏数据治理的成本永远低于事后排查。

### 9.5 灌入 RAGFlow

- **按业务域建多个知识库**,不要一个库装天下。
- **md 文件走 General + 按标题 + 父子分块**;**不要再叠加 PDF/Manual 解析**(md 已是解析结果)。
- 参数:中文 `chunk_token_num` 256~512 实测;`overlap` 10~15%。
- **用 API 批量灌**(不手点),写入元数据。
- **Embedding 模型一旦选定不可改**,先定好再灌(中文建议 BGE 系列)。
- **开混合检索 + Rerank**。
- 灌完**可视化抽查 chunk**,不对就重切这个库。

### 9.6 验收

- 黄金评测集 100~300 题。
- 检索命中率 **Recall@5 ≥ 90%**;端到端按场景定目标;拒答正确。
- 分层归因 + 上线后持续回灌 badcase。

---

## 第 10 章 端到端落地路线图(9 个阶段)

> 用图书馆类比:阶段1 搞清读者要问什么 → 阶段2 分类扔烂书 → 阶段3 准确抄成电子文字 → 阶段4 切卡片编索引上架 → 阶段5 快速找卡片组织答案 → 阶段6 控制借阅权限 → 阶段7 用考题检验 → 阶段8 开小窗口试运营 → 阶段9 持续补书改进。

### 阶段一 需求与目标定义(最该认真,最常被跳过)

**目标**:把"我要做个 RAG"变成"给谁、答什么、做到什么算成功"。

**怎么做**:
1. 定义用户和场景,列出最高频的 20 个真实问题。
2. 划定边界:明确写下**回答什么、不回答什么**。
3. 把"准确率"翻译成分层指标:Recall@5 ≥ 90%;端到端按问题类型定;拒答正确率 ≥ 95%。
4. 明确硬约束:数据是否出境?是否私有化?预算?延迟?

**✅ 完成标准**:有签字认可的《需求与边界说明》;"成功"是具体数字;四个硬约束已确认。
**⚠️ 坑**:跳过直接干;把"95%"当单一目标却不定义。

### 阶段二 数据盘点与治理

**目标**:把"乱文档"变成"分好类、去过重、标好签的干净资产"。

**怎么做**:建 `manifest.csv`;去重 + 多版本只留最新;删低价值;判定扫描件;设计元数据规范;规划知识库拆分。

**✅ 完成标准**:清单完整;去重去旧版完成;每份文档标好元数据和解析路线;拆库方案确定。
**⚠️ 坑**:多版本全入库;全塞一个库;不盘点直接灌。

### 阶段三 解析与数据加工(失败率最高)

**目标**:把各种格式准确变成结构清晰的文本。

**怎么做**:
1. 按难度分流:规整件用 DeepDoc/MarkItDown;难件用 MinerU(RAGFlow 内置可选);表格/FAQ 直接用 Table/Q&A 模板。
2. "要不要转 Markdown":默认不需要,仅特定理由才对难件走外部转换(见第 8 章)。
3. 解析质量门禁:自动检查(乱码/提取率/表格/空文件)+ 5% 人工抽样,**合格率 ≥ 90% 才放行**。

**✅ 完成标准**:每类定了解析方式;过了质检;难件解析质量经验证。
**⚠️ 坑**:无脑全量过 MinerU;把 Excel/FAQ 压成 md 丢结构;不质检直接灌烂数据。

### 阶段四 灌入知识库(切分 + 向量化 + 索引)

**目标**:切成语义完整、大小合适的卡片,建好索引。

**怎么做**:选切分模板;调参数(chunk_token_num 256~512、overlap 10~15%、父子分块);选并锁定 Embedding;API 批量灌 + 写元数据;灌完可视化抽查 chunk;高级增强(RAPTOR/GraphRAG)默认别开。

**✅ 完成标准**:模板参数选对;Embedding 统一;批量灌完元数据正确;chunk 抽查合格。
**⚠️ 坑**:默认参数不调;md 又叠 PDF 解析;无脑开 RAPTOR/GraphRAG。

### 阶段五 检索与生成(核心链路)

**目标**:找到对的内容并组织成准确、可信、带出处的答案。

**怎么做**:开混合检索;开 Rerank;查询理解(多轮/路由/改写);元数据过滤;生成层(提示词约束 + 溯源 + 拒答)。

**✅ 完成标准**:混合检索 + Rerank 启用;多轮/路由/改写按需配;答案带出处且能拒答。
**⚠️ 坑**:只用向量检索;不开 rerank;不拒答致幻觉;答案不给出处。

### 阶段六 权限、安全与合规(企业一票否决,技术团队最易漏)

**目标**:该看的能看,不该看的看不到,数据合法合规。

**怎么做**:文档级权限过滤(向量检索不天然懂权限,要在检索层显式加);接入 SSO/AD;PII 脱敏与密级;私有化决策(本地 embedding + 本地 LLM);审计日志。

**✅ 完成标准**:用不同身份账号验证过权限;合规脱敏到位;审计日志可追溯。
**⚠️ 坑**:整个忘记权限层 → 泄密;以为 RAGFlow 自带权限就够(粒度往往不够)。

### 阶段七 评测与验收(贯穿全程的标尺)

**目标**:用客观数据判断好坏、防止改动帮倒忙。

**怎么做**:建黄金评测集(100~300 题);自动化评测(RAGAS/DeepEval);分层归因;回归测试。

**✅ 完成标准**:评测集建好;能跑分层指标;达到阶段一的验收目标。
**⚠️ 坑**:没评测集靠感觉;只看一个模糊端到端数字。

### 阶段八 试点上线(小步快跑)

**目标**:在真实用户中小范围验证。

**怎么做**:选一个高价值、问题集中的场景试点;确定交付形态(网页/企微/钉钉/飞书/API);加安全护栏(输入拦截、输出审核、免责声明、转人工);灰度放量;答案旁放 👍/👎 收集 badcase。

**✅ 完成标准**:试点跑通达标;护栏兜底就位;反馈机制上线。
**⚠️ 坑**:一上来全量全员推广,信任一次崩塌难挽回。

### 阶段九 运营与持续迭代(RAG 是养出来的)

**目标**:让系统持续变好、不腐烂。

**怎么做**:反馈闭环(badcase → 归因 → 修 → 回灌 → 回归);文档生命周期管理(新增/改版/过期/重索引,可接 Confluence/S3 同步);监控(命中率/延迟/成本/失败率/拒答率);成本管理;**指定 owner**。

**✅ 完成标准**:闭环在运转;文档更新有流程;监控成本看板就位;有责任人。
**⚠️ 坑**:当一次性项目交付完不管;没人维护;不回归测试越改越差。

---

## 第 11 章 最容易被忽视的 60%(权限/安全/运营等)

我们大量篇幅在讲"数据加工 → 灌库",但那只是落地的约 40%。下面是另外 60%——**缺一个就可能翻车**,且技术团队最容易漏:

| 缺口 | 为什么致命 | 在哪章 |
|---|---|---|
| **权限 / 安全 / 合规** | 漏了可能数据泄密,企业一票否决 | 阶段六 |
| **需求与边界定义** | 不定义就无法验收,后面全是赌 | 阶段一 |
| **生成层:拒答 + 溯源 + 幻觉控制** | 直接决定"95%"和用户信任 | 阶段五 |
| **持续运营与反馈闭环** | RAG 不运营就会腐烂 | 阶段九 |
| **RAG 能力边界 / 预期管理** | 不划边界,目标本身就错了 | 第 12 章 |
| **文档更新生命周期** | 静态能跑,动态会乱 | 阶段九 |
| **成本 / 性能 / 规模化** | 上线后才暴雷(单次成本×QPS=月成本;上万份→几十万 chunk→向量库选型;延迟预算;并发) | 阶段四/八 |
| **护栏 / 交付集成** | 体验和风险层面 | 阶段八 |

---

## 第 12 章 RAG 能力边界(必须管理预期)

作为独立审查者必须说清:**有些问题 RAG 天生做不好**,别指望靠切分/解析解决。要么划到边界外拒答,要么换技术:

| 做不好的问题类型 | 例子 | 替代方案 |
|---|---|---|
| 需要计算/统计 | "所有项目的总预算是多少" | Text2SQL / Agent + 工具 |
| 需要全局归纳 | "总结这 500 份合同的共性" | RAPTOR/GraphRAG 部分缓解,有限 |
| 多文档对比、时间推理、强逻辑推理 | "对比近三年政策变化" | 易错,需专门设计 |
| 时效性强 | "今天的库存" | 文档不更新答案就过时 |

> **把这些硬塞给 RAG 并要求 95%,是注定失败的。**

---

## 附录 A 工具选型速查

| 用途 | 推荐 | 备注 |
|---|---|---|
| 一体化 RAG 平台 | RAGFlow(解析强)/ Dify(编排强)/ 扣子(轻量) | 数据敏感选私有化 |
| 难件解析 | MinerU(GPU,扫描/公式/表格) | RAGFlow 已内置,AGPL 系许可证需核实 |
| 轻量格式转换 | MarkItDown(MIT,纯 CPU,格式广) | 简单件批量转 |
| Embedding | BGE 系列(中文强) | **选定后不可改** |
| 向量库 | Milvus / Qdrant(规模大)/ PGVector(已有PG)/ ES(天然混合检索) | 上万份→几十万 chunk,选能扛规模的 |
| 重排 | BGE-reranker / Cohere Rerank | 强烈建议必上 |
| 评测 | RAGAS / DeepEval / TruLens | 建评测集必备 |
| 编排框架(自建) | LlamaIndex(专注 RAG)/ LangChain | — |

---

## 附录 B 落地优先级(精力该往哪投)

1. 🔴 **需求边界定义 + 评测集**(没这俩,后面全是赌)
2. 🔴 **数据治理 + 解析质量**(决定准确率上限)
3. 🔴 **权限合规**(漏了会泄密)
4. 🟠 **混合检索 + Rerank + 拒答溯源**(性价比最高的准确率提升)
5. 🟠 **持续运营闭环**(决定能不能长期活)
6. 🟡 **切分参数调优、RAPTOR/GraphRAG**(锦上添花,别一开始就纠结)

---

## 附录 C 避坑总纲

1. **数据干净 > 算法花哨**:80% 的失败来自解析和脏数据,不是切分算法。
2. **先小后大,用数据验收**:先试点、建评测集,再全量;拒绝"我感觉变好了"。
3. **RAG 是运营,不是工程**:建完只是开始,持续喂数据、修 badcase、管文档才能活。
4. **"95%" 必须先拆解**:分成"检索命中率"和"端到端正确率"分别定义,否则是无法验收的空指标。
5. **能用专用模板/原生结构就别压平**:Excel 用 Table、FAQ 用 Q&A,别无脑转 Markdown。
6. **别无脑全量过 MinerU**:按难度分流;RAGFlow 已内置 MinerU。
7. **别叠加双重解析**:md 进 RAGFlow 只走 General,不要再选 PDF/Manual 解析。

---

> 本手册基于公开资料与实际落地经验整理,产品功能(尤其 RAGFlow / MinerU / MarkItDown)迭代极快,具体参数与能力请以官方最新文档为准,并**务必用你自己的评测集在你自己的数据上验证**。
